{
  "name": "scytale-passbook",
  "private": true,
  "workspaces": [
    "pc",
    "mobile"
  ],
  "scripts": {
    "dev:pc": "npm --prefix pc run tauri dev",
    "dev:mobile": "npm --prefix mobile start",
    "build": "tsc -b && vite build",
    "build:pc": "npm --prefix pc run build && npm --prefix pc run tauri build",
    "build:mobile": "npm --prefix mobile run build 2>/dev/null || npx --prefix mobile expo export",
    "build:all": "npm run build:pc && npm run build:mobile"
  }
}
